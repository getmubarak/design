interface/implements -> get the classes into the same family (abstract programming)
inheritance/extends -> get the classes into the same family (abstract programming)
	            + common code

fun(List list){
	list.remove(3);
}

fun(stack);
